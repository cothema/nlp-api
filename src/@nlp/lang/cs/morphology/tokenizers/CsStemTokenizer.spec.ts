describe("CsStemmerTokenizer", () => {
  test("Tokenize", () => {
    // const tokenizer = new CsStemTokenizer();
    // expect(tokenizer.tokenizeToValues("výstavba")[0]).toBe("výstavb");
    // expect(tokenizer.tokenizeToValues("krasojízda")[0]).toBe("krasojízd");
    // expect(tokenizer.tokenizeToValues("výstavba")[0]).toBe("stav");
    // expect(tokenizer.tokenizeToValues("výstavní")[0]).toBe("stav");
  });
});
